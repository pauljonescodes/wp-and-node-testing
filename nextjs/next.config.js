/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    basePath: "/app",
    assetPrefix: "/app/",
    /* rest of the configuration */
};

module.exports = nextConfig;